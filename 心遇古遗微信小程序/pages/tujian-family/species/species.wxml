<view>
  <block wx:for="{{speKey}}" wx:for-item="item">
    <view class="family" catchtap="toDetail" data-spe-id="{{item.speId}}">
      <image src="{{item.speImg}}"></image>
      <text>{{item.speName}}</text>
    </view>
  </block>
</view>